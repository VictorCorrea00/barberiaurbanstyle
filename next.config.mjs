/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Disabled for dev stability, enable only for production build
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
